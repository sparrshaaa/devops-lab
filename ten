git init
git add .
git commit -m "first commit"
git branch -M main

git config --global user.name ""
git config --global user.email ""

ssh-keygen -t rsa -b 4096 -C *email*
cat ~/.ssh/id_rsa.pub

git remote add origin *url-of-rep*
git remote set-url origin *url-of-rep*
git push --set-upstream origin main
git push


-----------------
Azure -azure organization - my org - open 
new project 
	pipeline - new pipeline - GitHub YAML ->choose your proj
		approve and install
Build system -> Maven -> Save and run
JOB (successful)
		
